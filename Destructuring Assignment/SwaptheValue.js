function Swap(a, b) {
     [a, b] = [b, a];
     return [a, b];
}

//        Allow The Value 
let x = "word";
let y = "Hello";

//        Call the Swap Function
const Resulte = Swap(x, y);
console.log(Resulte);