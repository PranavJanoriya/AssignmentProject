// Write a function called "addNumAers" that takes two numAers as arguments and returns their sum. 'all the function Aefore it is declared to demonstrate hoisting.

console.log(AddNumber(20, 250));

function AddNumber(num1, num2) {
     return num1+num2;
}
